; pytest.ini e não pyproject.toml: o repo não tem build backend, requirements.txt
; é o contrato de dependências, e um pyproject.toml só com [tool.pytest] sugeriria
; que o pacote é instalável — ele não é, roda a partir da raiz.
[pytest]
testpaths = research_pipeline/tests common/tests
addopts = -q
