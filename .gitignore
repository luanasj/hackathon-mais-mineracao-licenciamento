# dependências
node_modules/
.venv/
__pycache__/
*.pyc

# artefatos de build
dist/
*.tsbuildinfo

# intermediários do pipeline (regeneráveis por `python pipeline/prep.py`)
frontend/public/data/*.raw.geojson

# sistema
.DS_Store

# cache do Vite
.vite/

data/db/*.db
data/db/*.db-journal

# Segredos — .env.* pega .env.local, .env.production e o que mais aparecer;
# a negação mantém o template rastreado.
.env
.env.*
!.env.example

# Testes e artefatos de run (§9 do GOAL.md)
.pytest_cache/
research_pipeline/runs/
